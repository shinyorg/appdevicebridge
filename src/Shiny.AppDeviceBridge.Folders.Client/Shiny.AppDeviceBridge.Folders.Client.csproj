<Project Sdk="Microsoft.NET.Sdk">

  <Import Project="../BridgeClient.props" />

  <PropertyGroup>
    <RootNamespace>Shiny.AppDeviceBridge.Folders.Client</RootNamespace>
    <Description>Typed client and contracts for the Shiny.AppDeviceBridge folders bridge — the platform's folder picker, and picked folders remembered as file roots — for a web app hosted by Shiny.AppDeviceBridge. Register with services.AddFoldersBridgeClient().</Description>
  </PropertyGroup>

</Project>
