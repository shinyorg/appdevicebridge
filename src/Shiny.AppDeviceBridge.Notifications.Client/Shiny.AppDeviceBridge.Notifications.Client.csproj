<Project Sdk="Microsoft.NET.Sdk">

  <Import Project="../BridgeClient.props" />

  <PropertyGroup>
    <RootNamespace>Shiny.AppDeviceBridge.Notifications.Client</RootNamespace>
    <Description>Typed client and contracts for the Shiny.AppDeviceBridge notifications bridge — access, sending and scheduling local notifications, the badge, channels, and taps as events — for a web app hosted by Shiny.AppDeviceBridge. Register with services.AddNotificationsBridgeClient().</Description>
  </PropertyGroup>

</Project>
