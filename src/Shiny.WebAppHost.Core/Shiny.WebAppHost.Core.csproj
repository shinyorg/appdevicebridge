<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Shiny.WebAppHost</RootNamespace>
    <Description>The update protocol shared by Shiny.WebAppHost.Maui and Shiny.WebAppHost.AspNetCore — release contracts, version ordering, and the ECDSA release signature both sides agree on.</Description>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Shiny.WebAppHost.Tests" />
  </ItemGroup>

</Project>
