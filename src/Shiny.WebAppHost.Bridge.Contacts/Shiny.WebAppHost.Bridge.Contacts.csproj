<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- Cleared: src/Directory.Build.props sets the single target. -->
    <TargetFramework></TargetFramework>

    <!-- Multi-targeted because Shiny.Contacts only ships AddContactStore in its Android and iOS builds. Every
         other head resolves net10.0, where the endpoints answer 501. -->
    <TargetFrameworks>net10.0;net10.0-android;net10.0-ios</TargetFrameworks>

    <UseMaui>true</UseMaui>
    <SingleProject>false</SingleProject>
    <RootNamespace>Shiny.WebAppHost.Bridge.Contacts</RootNamespace>
    <Description>Device contacts for a Shiny.WebAppHost web app, backed by Shiny.Contacts — access, paged search, read, photos, create, update and delete. One call: builder.AddContactsBridge().</Description>

    <SupportedOSPlatformVersion Condition="$(TargetFramework.Contains('-android'))">21.0</SupportedOSPlatformVersion>
    <SupportedOSPlatformVersion Condition="$(TargetFramework.Contains('-ios'))">15.0</SupportedOSPlatformVersion>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Maui.Controls" />
    <PackageReference Include="Shiny.Contacts" />
  </ItemGroup>

  <ItemGroup Condition="$(TargetFramework.Contains('-android')) OR $(TargetFramework.Contains('-ios'))">
    <PackageReference Include="Shiny.Hosting.Maui" />
    <Compile Include="../Shared/ShinyMauiHosting.cs" Link="ShinyMauiHosting.cs" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../Shiny.WebAppHost/Shiny.WebAppHost.csproj" />
  </ItemGroup>

</Project>
