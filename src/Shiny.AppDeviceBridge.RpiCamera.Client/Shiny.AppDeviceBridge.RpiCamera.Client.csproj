<Project Sdk="Microsoft.NET.Sdk">

  <Import Project="../BridgeClient.props" />

  <PropertyGroup>
    <RootNamespace>Shiny.AppDeviceBridge.RpiCamera.Client</RootNamespace>
    <Description>Typed client and contracts for the Shiny.AppDeviceBridge Raspberry Pi camera bridge — cameras, JPEG snapshots, captures into a file root, sensor controls and a live MJPEG stream — for a web app or tool calling a device running Shiny.AppDeviceBridge. Register with services.AddRpiCameraBridgeClient().</Description>
  </PropertyGroup>

</Project>
