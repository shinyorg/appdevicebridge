<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Shiny.WebAppHost.AspNetCore</RootNamespace>
    <Description>The release server for Shiny.WebAppHost — answers update checks with a signed release, decides required versus optional from a minimum version, and serves the zips. Releases come from a directory out of the box, or from any store you implement.</Description>
  </PropertyGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../Shiny.WebAppHost.Core/Shiny.WebAppHost.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Shiny.WebAppHost.Tests" />
  </ItemGroup>

</Project>
