<Project Sdk="Microsoft.NET.Sdk">

  <Import Project="../BridgeClient.props" />

  <PropertyGroup>
    <RootNamespace>Shiny.AppDeviceBridge.Wifi.Client</RootNamespace>
    <Description>Typed client and contracts for the Shiny.AppDeviceBridge Wi-Fi bridge — capabilities, scanning, the current network, joining and forgetting networks, the radio and a hotspot — for a web app hosted by Shiny.AppDeviceBridge. Register with services.AddWifiBridgeClient().</Description>
  </PropertyGroup>

</Project>
