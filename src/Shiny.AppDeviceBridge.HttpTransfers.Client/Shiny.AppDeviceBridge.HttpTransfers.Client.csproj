<Project Sdk="Microsoft.NET.Sdk">

  <Import Project="../BridgeClient.props" />

  <PropertyGroup>
    <RootNamespace>Shiny.AppDeviceBridge.HttpTransfers.Client</RootNamespace>
    <Description>Typed client and contracts for the Shiny.AppDeviceBridge HTTP transfers bridge — background downloads into and uploads from the page's file roots, with progress and completion events — for a web app hosted by Shiny.AppDeviceBridge. Register with services.AddTransfersBridgeClient().</Description>
  </PropertyGroup>

</Project>
