<Project Sdk="Microsoft.NET.Sdk">

  <Import Project="../BridgeClient.props" />

  <PropertyGroup>
    <RootNamespace>Shiny.AppDeviceBridge.Calendar.Client</RootNamespace>
    <Description>Typed client and contracts for the Shiny.AppDeviceBridge calendar bridge — calendars, events in a date range, create, update and delete — for a web app hosted by Shiny.AppDeviceBridge. Register with services.AddCalendarBridgeClient().</Description>
  </PropertyGroup>

</Project>
