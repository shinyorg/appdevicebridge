<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Shiny.AppDeviceBridge.AspNetCore</RootNamespace>
    <Description>The release server for Shiny.AppDeviceBridge — answers update checks with a signed release, decides required versus optional from a minimum version, and serves the zips. Releases come from a directory out of the box, or from any store you implement.</Description>
  </PropertyGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../Shiny.AppDeviceBridge.Core/Shiny.AppDeviceBridge.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Shiny.AppDeviceBridge.Tests" />
  </ItemGroup>

</Project>
