<Project Sdk="Microsoft.NET.Sdk">

  <Import Project="../BridgeClient.props" />

  <PropertyGroup>
    <RootNamespace>Shiny.AppDeviceBridge.Health.Client</RootNamespace>
    <Description>Typed client and contracts for the Shiny.AppDeviceBridge health bridge — HealthKit and Health Connect permissions, bucketed and record reads, writes and live readings — for a web app hosted by Shiny.AppDeviceBridge. Register with services.AddHealthBridgeClient().</Description>
  </PropertyGroup>

</Project>
