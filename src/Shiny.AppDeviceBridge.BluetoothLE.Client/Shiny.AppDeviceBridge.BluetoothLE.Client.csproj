<Project Sdk="Microsoft.NET.Sdk">

  <Import Project="../BridgeClient.props" />

  <PropertyGroup>
    <RootNamespace>Shiny.AppDeviceBridge.BluetoothLE.Client</RootNamespace>
    <Description>Typed client and contracts for the Shiny.AppDeviceBridge Bluetooth LE bridge — scanning, connections, services, characteristic reads, writes and notifications — for a web app hosted by Shiny.AppDeviceBridge. Register with services.AddBluetoothLEBridgeClient().</Description>
  </PropertyGroup>

</Project>
