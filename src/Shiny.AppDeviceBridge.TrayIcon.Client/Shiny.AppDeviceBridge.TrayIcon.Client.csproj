<Project Sdk="Microsoft.NET.Sdk">

  <Import Project="../BridgeClient.props" />

  <PropertyGroup>
    <RootNamespace>Shiny.AppDeviceBridge.TrayIcon.Client</RootNamespace>
    <Description>Typed client and contracts for the Shiny.AppDeviceBridge tray icon bridge — icons, badges, menus, notifications, animations and click events on Windows, macOS and Linux — for a web app hosted by Shiny.AppDeviceBridge. Register with services.AddTrayBridgeClient().</Description>
  </PropertyGroup>

</Project>
